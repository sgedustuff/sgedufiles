1) python --version
2) pip --version = It is the package manager which is installed with python. It is genrally used to install the packages or libraries or plugin like python pandas. there are lot of things which are used in python, so everthing is not come up along with the installation of python. We use PIP, to install as per the requirements.
3) pip install pandas = installing pandas.
4) ctrl + N = new file
5) F5 = execute.
6) exit() = come out from output screen.
